# Dye Hard

Clean browser build for GitHub Pages and jsDelivr delivery.

## Use

Host the repository with GitHub Pages or load it through jsDelivr. The entry file is `index.html`, and all game paths are relative so the build can run from a GitHub Pages URL, a jsDelivr URL, or an embedded page on another website.

No ad, tracking, cookie consent, or portal mirror files are required for this build.